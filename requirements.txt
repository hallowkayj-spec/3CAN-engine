# 3CAN v0.2 release-candidate runtime dependencies
# Python >= 3.11 required (tested on 3.11, 3.12, 3.14)

# ---- Core HTTP + async ----
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
requests>=2.31,<3.0
httpx>=0.28,<1.0

# ---- Embedding + retrieval ----
# Linux/WSL CPU-only machines should install torch from the PyTorch CPU index
# before this file, otherwise pip may try to pull multi-GB CUDA wheels:
# python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
sentence-transformers>=2.7,<4.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0

# ---- Community detection (optional but recommended) ----
# Comment out if installation fails on your platform; 3CAN will degrade gracefully
leidenalg>=0.10
python-igraph>=0.11

# ---- Reranker (optional, bge-reranker is primary, FlashRank is fallback) ----
# Primary reranker is loaded via sentence-transformers above; FlashRank only used if primary fails:
flashrank>=0.2

# ---- Dev / lint (optional) ----
# Install with: pip install -r requirements-dev.txt
# ruff>=0.4
# pytest>=8.0
