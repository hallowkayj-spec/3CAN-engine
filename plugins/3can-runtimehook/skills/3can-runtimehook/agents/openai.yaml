interface:
  display_name: "3CAN RuntimeHook"
  short_description: "中文保持主目标、临时任务自动收尾和阶段复核，减少开发漂移"
  default_prompt: "使用 $3can-runtimehook 保持当前目标；临时任务完成后清除临时状态并返回主任务。"
policy:
  allow_implicit_invocation: true
